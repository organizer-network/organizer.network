<% if (person) { %>
	<%- include('intro', {
		person: person,
		contexts: contexts
	}) %>
<% } else { %>
	<%- include('login', {
		context: contexts.current,
		preview_link: false
	}) %>
<% } %>
<% if (contexts.current) { %>
	<%- include('context', {
		person: person,
		context: contexts.current
	}) %>
<% } %>
